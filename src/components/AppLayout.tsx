// src/components/AppLayout.tsx
import React from 'react';
import CartPilotApp from './QuickShopApp'; // We renamed but kept the same file

const AppLayout: React.FC = () => {
  return <CartPilotApp />;
};

export default AppLayout;